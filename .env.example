# Optional: real Space-Track credentials for future live adapter
SPACE_TRACK_USERNAME=
SPACE_TRACK_PASSWORD=

# Optional: Vercel KV for persistent view counts (falls back to CountAPI if unset)
KV_REST_API_URL=
KV_REST_API_TOKEN=